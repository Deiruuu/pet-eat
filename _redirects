# Rule for SPA client-side routing (for pet details pages)
/pet/* /index.html 200

# Fallback for all other paths (ensures index.html is served for other SPA routes, and static assets if they were external)
/* /index.html 200
